# Project agent memory

This file is the project's committed home for project-intrinsic agent knowledge: build, test, release, architecture, and sharp-edge notes that should travel with the code.

- Add durable project-specific notes here as they are discovered through real work.

## Runtime

The native Swift cockpit (`native/`) is the only app in this repo. The old Python/FastAPI backend, its WKWebView shell (`desktop.py`), and its py2app packaging (`build_app.sh`, `setup.py`, `pyproject.toml`) were removed once the native app became feature-complete - see `native/README.md` for build/run instructions.

## Native app (`native/`, Swift + SwiftTerm)

- The sole cockpit surface. Build with `swift build` / run with `swift run` from `native/` (Command Line Tools only, **no Xcode / `xcodebuild`**). First build is ~90s. Full run + captain-validation steps live in `native/README.md`; the authoritative design + verified API shapes are in `data/cockpit-native-design-scout/report.md` (sections 3, 4.3, 5, 6, 7, 9).
- **SwiftTerm is vendored, not a remote SPM dependency** (`native/Vendor/SwiftTerm/`, pinned to upstream 1.15.0 / commit `dd2fb8ac...`; `Package.resolved` no longer exists, since there are no remote dependencies left). This is a deliberate patch, not a stylistic choice: SGR-2 (dim/faint) text rendering (`NSColor.dimmedColor(towards:)` in `Mac/MacExtensions.swift`, mirrored in `iOS/iOSExtensions.swift`) blended a flat 50% toward the background regardless of whether that background was dark or light - fine on dark themes (~4.9:1 contrast) but collapsing to ~2.9:1, nearly invisible, on light ones - and none of `dimmedColor`, `getAttributes`, `buildAttributedString`, or `mapColor` are `open` (nor is `TerminalView.draw(_:)`), so there is no override point from outside the module. The patch (new file `Dimming.swift`) targets a fixed 4.5:1 WCAG contrast ratio against the background via bisection, capped at the original 50% so dark themes render unchanged. See `native/Vendor/SwiftTerm/README.md` for the full writeup and how to re-apply the patch if SwiftTerm is ever upgraded.
- **Second SwiftTerm patch: truecolor de-emphasised text (cockpit-native-fixes5).** The `dimmedColor` patch above only fires for the SGR-2 "faint" attribute - some tools de-emphasise text via a literal 24-bit truecolor foreground instead (`ESC[38;2;r;g;bm`), which carries no theme awareness at all since the child process chose concrete RGB bytes with no idea what background they'd land on. Confirmed live via `tmux capture-pane -e` on a real firstmate session: Claude Code renders its own status lines ("Searched for N files...") as `ESC[38;2;153;153;153m`, not SGR-2 dim - ~6.7:1 contrast on a dark theme (fine) but ~2.55:1 on light (illegible), and `getAttributes`'s `.dim` branch never touches it. `getAttributes` (`Apple/AppleTerminalView.swift`) now also checks `attribute.fg` for the `.trueColor` case and calls the new `NSColor.legibleColor(against:)`/`UIColor.legibleColor(against:)` (`Mac/MacExtensions.swift` / `iOS/iOSExtensions.swift`, backed by `Dimming.contrastFixBlendFraction`), which remaps *any* truecolor foreground that doesn't already meet the contrast floor - self-gating (no-op once contrast is sufficient) and hue-preserving (blends toward whichever of black/white the color can reach the most contrast against, not simply "toward its own background" like the dim patch - that direction has to be chosen by evaluating both endpoints, since a foreground already close to one extreme has almost no headroom left in that direction). See `native/Vendor/SwiftTerm/README.md`'s "Second patch" section.
- **The console** (`main.swift` -> `ConsoleController`) hosts a **dynamic tab collection**, not a fixed two-tab enum. Tabs are `[TabModel]` (`TabModel.swift`), each owning a `CockpitTerminalView` + a `TabLaunch` (re)start recipe, rendered as `TabChipView` chips in a growing tab bar. Supports **new** (`⌘T`/`+`), **duplicate** (`⌘D`, re-runs the same argv), **rename** (double-click / `⌘⇧R` / right-click, name is per-tab and never touches the process), **close** (`⌘W`; closing the last tab opens a fresh shell so the window is never empty), and `⌘1`…`⌘9` select. Tab ops are on the **Tab** menu; zoom+theme on **View**. `ConsoleController.openFirstmateHost()` opens the built-in Shell + Mirror pair; `loadView` calls it at startup so the app still lands there by default, and it is also the pinned Hosts-sidebar entry's connect action (see below). This is Phase 0 of the connection-manager work (design: `data/cockpit-ssh-manager-research/report.md`, Section A4/A5 + D Phase 0). Later phases add the dashboard + embedded backend (P3) and packaging/signing (P4); the console stays backend-free.
- **Navigation shell.** `AppShellController` is the window's root: a fixed, always-visible `IconRailController` (~60pt, icon-only) on the left, and to its right a `TopBarController` (destination title, the Search pill with its ⌘K badge, and the theme-picker button) above a body area. `RailDestination`'s case order (`IconRailController.swift`) drives the rail's top-to-bottom icon order directly - since Swift derives `CaseIterable.allCases` from declaration order, reordering the destinations is a one-line change there (currently `overview, console, hosts, review, settings`, below the sailboat "Home" mark and above the avatar - captain-corrected in cockpit-native-theme-audit-review, overriding fixes4 Fix 2's console/hosts-first order). The body holds all five destinations (`.overview`, `.hosts`, `.console`, `.review`, `.settings`) as permanent children with only `isHidden` toggled - never rebuilt - so navigating away never tears down `ConsoleController`'s running tabs. **`.hosts` and `.console` are decoupled destinations** (cockpit-native-ui-fixes2): `HostsSidebarController` and `ConsoleController` are each a standalone full-size body view, not panes of a shared `NSSplitViewController` - Console shows just the Firstmate Shell/Mirror tab strip, and Hosts is reachable on its own rail icon exactly like Settings. Connecting the pinned Firstmate entry (`HostsSidebarController.onConnectPinned`, wired in `main.swift`) opens its Shell/Mirror pair in the shared `ConsoleController` and calls `AppShellController.show(.console)`. Connecting a *saved* host goes through a sixth kind of destination outside the `RailDestination` enum entirely - see "Dedicated host pages" below. `.overview` shows `FleetController` (see below); `.review` shows `ReviewController` (cockpit-native-theme-audit-review): the same PR data/actions as Overview's "Ready to merge" (`OpenPRsSource.fetch()` + `FleetDataSource.mergedPRs`), grouped by forge instead of merged into one list; `.settings` shows `SettingsController` embedded directly (not a separate window).
- **Per-host pinned rail icons (fixes4 Fix 3).** Below the fixed destinations, `IconRailController.setHosts(_:)` renders one extra icon per saved host (its own icon glyph + accent, tooltip = label; click connects via `onConnectHost` exactly like the Hosts list's Connect). Kept live by `HostStore.observe(_:)` - a list of closures (not a single overwritable `onChange`, since both `HostsSidebarController.reload()` and the rail's `setHosts` need to hear every add/rename/delete) - `main.swift` registers the rail's observer alongside the Hosts panel's own.
- **Dedicated host pages (cockpit-native-host-pages).** A saved host's rail icon (or the Hosts list's own Connect - both call `AppDelegate.connectToHost`, so there is one connect path for a saved host, not two) opens its **own** page: an independent `ConsoleController` holding only that host's ssh tab(s), never the Firstmate console's Mirror/Shell tabs. `AppShellController.connectHost(_:args:)` builds one lazily per host id via an injected `makeHostConsole` factory (so `AppShellController` stays ignorant of `SSHKeyStore`/`SnippetStore`, matching how it already knows nothing about host persistence), embeds it into the shared body container exactly like a fixed `RailDestination`, and keeps it in a `[UUID: ConsoleController]` dictionary for as long as that host stays saved. The dedup fix for the "second click opens a second tab" bug: `ConsoleController.connectSSHIfNeeded` only calls `openSSH` when `tabs.isEmpty`, so a re-click of an already-connected host's icon just re-shows and re-focuses (`focusCurrentTab()`) the existing page - a deliberate second session still needs the tab chip's own Duplicate (`⌘D`). `ConsoleController(opensFirstmateOnLaunch:)` is `false` for these (the shared Firstmate `console` keeps it `true`, its Shell/Mirror pair unchanged). Deleting a host (`HostStore.observe` diffing ids in `main.swift`) tears its page down via `AppShellController.removeHostConsole` (`ConsoleController.shutdown()` + `removeFromSuperview`/`removeFromParent()`), and `applicationWillTerminate` shuts every host console down too (`AppShellController.shutdownAllHostConsoles()`), not just the shared one. None of this is a `RailDestination` case - `IconRailController.activeHostID`/`AppShellController.connectHost` track "a host page is showing" as a separate piece of state from the fixed-destination enum, mirrored on both sides so rail highlighting and body-view switching agree. Tab-menu actions (⌘T/⌘D/⌘W/zoom/theme, wired in `main.swift` with no explicit `.target`) still resolve correctly per page because each host's `ConsoleController` is a real `addChild` in the responder chain - whichever page's terminal is first responder is what the action reaches, exactly like the Firstmate console already worked.
- **Fleet dashboard (`FleetController.swift` + `FleetData.swift`, cockpit-native-ui-fixes2).** The `.overview` rail destination is a real port of `backend/fleet.py`'s `snapshot()` and `backend/openprs.py`'s `open_prs()` into Swift - greeting header, calm/loud answer banner, stat tiles (working/ready-to-merge/queued/done/projects/watcher), an "In flight" list of working crew, and a "Ready to merge" list of open PRs with Review/Merge actions. **No embedded backend**: `FirstmateHome.swift` resolves `$FM_HOME` the same way `backend/config.py` does, then `FleetData.swift` reads `data/projects.md` / `data/backlog.md` / `state/*.meta` directly and shells out to `bin/fm-crew-state.sh` per task (the same authoritative, non-tailed state read the Python backend uses) and to `bin/fm-pr-merge.sh` for the Merge action; `OpenPRsSource` walks `$FM_HOME/projects/*` and re-implements `openprs.py`'s remote-parsing + `gh pr list` + Bitbucket-REST logic natively. `FleetController.refresh()` runs this whole pipeline off the main thread and re-renders on completion - there is no live websocket push (unlike the web app), only a refresh on `viewWillAppear` plus a manual refresh button.
- **Hosts / SSH connection manager (Phase 1, `data/cockpit-ssh-manager-research/report.md` A2/A3, C1, D Phase 1).** `HostsSidebarController` (Termius-proportioned, 220-260pt, list, per-host SF-Symbol icon + Helm accent, quick-connect, add/edit/delete) is its own rail destination (see above); Add/Edit Host is a dedicated full-page window (`AppDelegate.presentHostEditor`), not a sheet on this narrow panel - `HostsSidebarController.onAddOrEdit` forwards the request up through `AppShellController` to the app delegate, which owns `HostEditorController` and the host store. Row zero is always a pinned, non-deletable "Firstmate" entry (anchor icon) whose connect action is `ConsoleController.openFirstmateHost()` - it has no backing `Host` record, so `HostsSidebarController`'s private `HostListRow` enum carries a `.pinned` case alongside `.header`/`.host`. A saved `Host` (`Host.swift`) is `Codable` and persisted by `HostStore` to `~/Library/Application Support/FirstmateCockpit/hosts.json` (override `FM_HOSTS_FILE`) - the native app's first on-disk persistence. **Secrets stay off disk:** `Host.password` is excluded from `CodingKeys` (session-only, memory); the only persisted credential is `keyID`, a reference into the Phase 2 key store below - never a path or key material. **Connect** for a saved host reaches its own dedicated page (see "Dedicated host pages" below) whose one ssh tab runs `/usr/bin/ssh` with the host's argv (the C1 drop-in) plus, when `keyID` is set, a `-i <path>` resolved fresh on every start (see below); the tab takes the host label + accent, and the tab chip's own Duplicate (`⌘D`) re-runs the same launch (re-resolving the key) for a deliberate second session. An ad-hoc quick-connect (`HostCatalog.parseQuickConnect`, no saved `Host` behind it) still opens as a plain tab in the shared Firstmate `ConsoleController` via `ConsoleController.openSSH(...)` directly, since there's no host id to pin a dedicated page to. The Hosts menu's Quick Connect routes through `AppShellController.revealHostsQuickConnect()`, which selects the Hosts destination before focusing it. The topbar Search control is unrelated to Hosts now - see "Find" below.
- **SSH keychain (Phase 2, `data/cockpit-ssh-manager-research/report.md` A1, C3, D Phase 2; Keychain model corrected in cockpit-native-fixes3).** `SSHKey.swift` is non-secret metadata only (label, type, derived public key, fingerprint, certificate), persisted by `SSHKeyStore` to `keys.json` (override `FM_KEYS_FILE`) exactly like `HostStore`. Private key bytes and passphrase live **only** in the macOS Keychain (`KeychainKeyStore.swift`: `SecItemAdd`/`SecItemCopyMatching`/`SecItemDelete`), stored with plain `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` (never iCloud-synced) - **not** a `SecAccessControl` ACL. That's a deliberate correction, not the original design: on a CLT-only, ad-hoc-signed build (`TeamIdentifier=not set`), `SecItemAdd` with *any* `kSecAttrAccessControl` fails `errSecMissingEntitlement`, confirmed with a standalone probe outside the app - see `KeychainKeyStore.swift`'s file header for the full writeup, including why a `keychain-access-groups` entitlement isn't a fix either (ad-hoc signing it gets the process killed outright, no provisioning profile). The Touch ID / passcode gate is therefore enforced in-app (`KeychainKeyStore.authenticate(context:)` calling `LAContext.evaluatePolicy` before the read), not by the OS ACL - revisit once the app has a real Team ID (Phase 4 packaging). Generation and PEM/OpenSSH import both shell out to `/usr/bin/ssh-keygen` (`SSHKeyGenerator.swift`) rather than reimplementing the OpenSSH key format or its passphrase KDF - `.ppk` files are detected and rejected with a conversion hint, not parsed. `SSHKeyStore.addNew(_:privateKeyData:passphrase:)` is the one path that writes a brand-new key (Keychain first, then metadata) - both the Keys screen and the host editor's inline "+ New Key…" popup entry (`HostEditorController.presentNewKeySheet`, still a sheet - now on the host editor's own window rather than the sidebar's) go through it, so a new key can be created without leaving the host form. At connect time `SSHKeyMaterializer.materialize` does the one Keychain read (the Touch ID prompt) and writes a private `0600` temp file for `ssh -i`; `ConsoleController.connectSSH`/`cleanupSSHKeyTempFile` own its lifecycle (fresh file per tab start/reconnect, deleted on close/exit/quit). The "SSH Keys" browser (`KeysSidebarController.swift` + `KeyEditorController.swift`) is a separate window (Keys menu, `⌘⇧K`/`⌘⇧N`), not a rail destination, and its footer/context-menu Delete action (`SSHKeyStore.delete(id:)`) removes both the metadata and the Keychain items - see `KeychainKeyStore.delete(id:)`. A `swift build`/`swift run` binary has no stable code-signing identity, so Keychain access can prompt again (or rarely fail to re-read) across rebuilds; `native/build_native_app.sh` now codesigns the assembled `.app` with a fixed local identity to keep that identity stable across rebuilds (see the `build_native_app.sh` bullet below and its "Local signing setup" section in `native/README.md`) - not a full Phase 4 Developer ID/notarization setup, just a per-machine local dev cert.
- **Power features (Phase 3, `data/cockpit-ssh-manager-research/report.md` B1/B2/B4/B5, D Phase 3).** All additive `ssh` argv or extra `Host` fields, never new transport code. Jump hosts: `Host.jumpVia` (a saved host's label or a raw `user@bastion`) resolved and chained by `HostCatalog.proxyJumpChain` into `-J a,b,c`. Agent forwarding: `Host.agentForward` -> `-A` (the system agent's `SSH_AUTH_SOCK` is already inherited via `childEnvironmentDict`). Known-hosts prompts: no code needed - `connectSSH` never overrides `StrictHostKeyChecking` and nothing filters the ssh tab's PTY output, so they render like any other terminal output; verified by inspection, not a UI. Groups/tags: `Host.group`/`Host.tags` (present since Phase 1) now drive sidebar section headers and a tag-chip filter row in `HostsSidebarController`. Port forwarding: `Host.portForwards: [PortForwardRule]`, edited via `PortForwardingController` as a sheet on top of `HostEditorController` (a window since the nav-redesign task, not a sheet itself - see above), turned into `-L`/`-R`/`-D` flags. Snippets: `Snippet`/`SnippetStore` (mirrors `HostStore`), managed from `SnippetsController` (Snippets menu, `⌘⌥N`/`⌘⌥P`); Run sends text+Enter to the active tab via `TerminalView.send(txt:)`; a host's `startupSnippetID` fires the same way ~1.5s after ssh starts (best-effort, no "shell ready" signal exists to hook). Session logging: `CockpitTerminalView` overrides SwiftTerm's `dataReceived(slice:)` to tee raw bytes to a `FileHandle` under `~/Library/Application Support/FirstmateCockpit/logs/` - deliberately not SwiftTerm's own `setHostLogging(directory:)`, which splits output across one file per read syscall rather than a single transcript. Toggle via `⌘⇧L`/toolbar icon, or default-on via `FM_LOG_SESSIONS_DEFAULT=1`.
- Every tab is a `CockpitTerminalView` (the P1 paste-hardening `LocalProcessTerminalView` subclass - do not touch its paste override; it is what makes screenshot-paste-into-Claude work). Terminals fork their child **in-process** via `startProcess`, so there is no websocket byte-pump and no per-keystroke round trip.
- **Scrolling:** SwiftTerm 1.15's `scrollWheel` already scrolls normal-screen buffers smoothly and content-wise (accumulates precise trackpad deltas -> whole lines 1:1, no page-jumps; `scrollSensitivity` default 1.0), so shell tabs feel like WezTerm with no extra code. Each terminal gets a **10,000-line scrollback** via `term.terminal?.changeScrollback(...)` (SwiftTerm default is 500). The Mirror tab is tmux on the alt-screen and pages inherently - do not try to force smooth-scroll there.
- **The Mirror tab now follows firstmate's own resolved runtime backend instead of assuming tmux unconditionally (`fm/cockpit-mirror-herdr-aware`, resolving the standing `cockpit-herdr-scout-decision-herdr-awareness` backlog item).** `FirstmateBackend.swift` is the single resolution point. **`fm/cockpit-mirror-backend-resolve-fix` corrected a live bug in it**: `resolve()` still sources the real `bin/fm-backend.sh` and calls its own `fm_backend_name` for an *explicit* `FM_BACKEND` env / `config/backend` override (never reimplementing that precedence), but no longer falls through to `fm_backend_name`'s ambient-marker (`HERDR_ENV=1`) auto-detection when neither is set - that auto-detection reads the *calling process's own environment*, which structurally never carries herdr's marker for this standalone GUI app (unlike a crewmate spawned from within firstmate's own already-running session), so it always silently landed on the `tmux` default even on a real herdr fleet. Absent an explicit override, `resolve()` now checks for live evidence instead - a real, running herdr session matching `herdrSessionName()` via `herdr session list --json` - and only then returns `.herdr`; see the file's own header comment for the full reasoning and why this must not be "simplified" back to a single `fm_backend_name` shell-out. `ConsoleController.connectMirror` calls `FirstmateBackend.resolve()` on every connect/reconnect and picks a `MirrorSession` case (`TabModel.swift`) accordingly:
  - **tmux** (unchanged): `TmuxMirror.swift` ports `backend/terminal.py`'s grouped-session lifecycle to Swift `Process` - same `new-session -d -s <group> -t <session>` / `select-window` / `window-size latest` / `status off` / teardown, same `cockpit_<sess>_<pid>_<hex>` naming. A crash (not a clean quit) can leave a stale `cockpit_*` group, same tradeoff as the Python version. **Known follow-up (still open, tmux-only):** `setUp` only calls `select-window` when the target names one explicitly (`firstmate:<N>`); with just a session name it inherits whichever window was active in the real session at that instant. Pin `firstmate:<N>` until there's a real fix - see the doc comment on `TmuxMirror.setUp`.
  - **herdr (`HerdrMirror.swift`) is a real `herdr session attach <session>` client, not a read-only mirror - and its tab is named "Herdr", not "Mirror" (`fm/cockpit-mirror-herdr-real-attach`, superseding the original `cockpit-mirror-herdr-aware` polling approximation described below in earlier history).** The original v1 shipped a `bash -c` loop polling `herdr pane read --format ansi` on a ~1.2s interval specifically because of an unresolved question: does a second concurrent `herdr session attach` disturb an already-attached client, the way a *non*-grouped tmux attach would? `fm/cockpit-mirror-herdr-real-attach` answered this live before changing anything - not by assumption - **and confirmed it's safe**: verified against a throwaway, disposable herdr session (`fm_test_attach_verify`, created and destroyed solely for this test, never the machine's real, live-in-use `default` session) that two independent `herdr session attach`/`herdr --session` clients coexist against one session with no disconnects, errors, or visible disruption to either - `herdr pane list` reported one shared pane serving both the whole time, the same shape as tmux's grouped-session model. With that confirmed, the fix is deliberately NOT a bespoke mirroring/rendering mechanism: `HerdrMirror.attachExecutable`/`attachArgs` are just `herdr session attach <session>` run through the exact same `LocalProcessTerminalView.startProcess` PTY path every other tab already uses (`ConsoleController.connectMirror` barely changed), so herdr's own client renders its own real UI (sidebar, tab bar, full interactivity) with zero custom rendering code here. The old pane-discovery heuristic (`firstMatePane`, `<session>#<pane-id>` pin syntax) is gone with the polling code that needed it - a real attach shows the WHOLE session, not one pane's text, so there's nothing to pick. `ConsoleController.connectMirror`'s herdr branch launches this tab with `currentDirectory: FirstmateHome.root.path` (not `shellCwd()`, which is the *shell* tab's own captain-configurable preference) - a deliberate, explicit call for this tab specifically. One incidental finding worth knowing before testing anything herdr-attach-related again: herdr refuses to launch when it sees its own `HERDR_ENV=1` marker in the environment ("nested herdr is disabled by default") - irrelevant for two independent GUI-launched clients, but `childEnvironmentDict()` (`TerminalEnvironment.swift`) now strips `HERDR_ENV`/`HERDR_SOCKET_PATH`/`HERDR_PANE_ID`/`HERDR_TAB_ID`/`HERDR_WORKSPACE_ID` from every terminal child's environment regardless, mirroring why `TMUX` is already stripped there.
  - `mirrorTarget()` (`TerminalEnvironment.swift`) still checks `FM_MIRROR_TARGET`, then Settings > General's "Mirror target" (`AppSettings.mirrorTarget` - field name unchanged, it's shared plumbing behind both backends' tab, not a UI label), before falling back to a backend-appropriate default: `firstmate` for tmux (unchanged), or `FirstmateBackend.herdrSessionName()` for herdr (a plain session name only now - no pane-pin syntax, see above).
  - Settings' session picker (`TmuxMirror.listSessions()`, see below) stays tmux-only - it was not extended to herdr, since the brief scoped that specifically to target resolution, not this tab's rendering.
  - `TabLaunch.defaultName` for `.mirror` is resolved fresh from `FirstmateBackend.resolve()` on every call (not cached) - "Mirror" for tmux, "Herdr" for herdr - so duplicate/rename/reconnect all pick the right label for whichever backend is live right now.
- **Ten confirmed AppKit gotchas worth knowing before touching tab chips, a scroll view, the host editor form, a dense row layout, or any full-size destination/window root.** (10) A horizontal `NSStackView` left at its default `.gravityAreas` distribution (never set explicitly) does not honor arranged-subviews' hugging/compression-resistance priorities to absorb slack width at all - those priorities only matter under `.fill`/`.fillProportionally`/`.fillEqually`. Under `.gravityAreas`, all views added via the plain `views:` initializer or `addArrangedSubview` land in the *center* gravity area and get laid out at their natural size with no defined "who grows" rule, so leftover width is resolved by Auto Layout's own tie-breaking - which can drift between runs/rows depending on transient sibling content (a spinner swapped for a button, a longer status string) even with no code change. This is what caused the Updates page's per-row disclosure chevron (`UpdatesController.buildRow`'s `topRow`) to sit flush against the trailing edge for some rows and stop short with a stray gap for others, inconsistently, after expanding a row's log. There's a second, compounding trap: even under `.fill`, an `NSStackView` container's *own* horizontal hugging priority (not its children's) defaults lower than any priority you set on its arranged-subview children - so a `trailingStack` of `.required`-hugging buttons/pills can still itself get chosen to absorb the row's slack instead of the intended flexible text label, unless you also set `.required` hugging/compression-resistance on the container view itself. Fix needs both: set `topRow.distribution = .fill` AND give every non-flexible arranged subview (including any nested `NSStackView` container, not just its children) `.required` hugging/compression resistance, leaving only the one view meant to flex (the title/detail text stack) at `.defaultLow`. Confirmed live via a temporary `FM_DEBUG_CHEVRON`-style env-gated probe (`performClick(nil)` on the disclosure button + `ThemeManager.shared.setTheme` cycling, dumping `NSView.frame` for the chevron/trailing-stack/text-stack after each) - reverted before commit, per the "Verifying native UI bugs" convention below. Same root cause hit a third time (cockpit-bootstrap-row-width-parity): `BootstrapController.buildStepRow`'s `row` (a horizontal `[leftColumn, bodyStack]` stack wrapping each stepper step's content box) also left `.gravityAreas` distribution unset, so `bodyStack` - and therefore the `stepContentBox` nested inside it - stayed shrunk to its content's natural width even though the step's own outer card correctly filled the page (the card's `background` *was* already width-tied to the page via an external `widthAnchor.constraint(equalTo: stack.widthAnchor)`, so the empty gap looked like a "card too narrow" bug but was actually this same nested-row distribution gap one level in). Confirmed live via an `FM_DEBUG_BOOTSTRAP_WIDTH`-style probe dumping `stepContentBox` frame widths before/after the fix (169-467pt narrow -> 1044pt full-width, tracking window resize correctly afterward) - fixed by adding `row.distribution = .fill` plus `.required` hugging/compression resistance on `leftColumn` and `.defaultLow` on `bodyStack`, the same shape as the Updates-page fix above. (1) `TabChipView.beginRename()` (fixes4) used to call `window.makeFirstResponder(label)` *and then* `label.selectText(nil)` - `selectText(nil)` alone already makes an editable field first responder and starts editing, so the redundant prior `makeFirstResponder` call makes AppKit think a session is already active and needs ending before `selectText` starts its own. That fires a spurious `controlTextDidEndEditing` with the *pre-edit* text right there, permanently flipping the "is renaming" flag false before the user types a single character - the rename UI looks like it's working, but the real commit on Return later hits a now-stale guard and is silently dropped. Root-caused via `Thread.callStackSymbols` inside the delegate callback, not by reading the code. Never call both; `selectText(nil)` is sufficient on its own. (2) `NSGridView` column widths: setting an explicit pixel width on one column (e.g. the field column) while leaving the other unconstrained does NOT make the constrained column absorb extra space on window resize - the *unconstrained* column absorbs 100% of any extra width instead, since nothing else defines where it should stop. That's what caused the "Add Host" form's labels to drift into a growing empty gap as the window widened. Fix: give the column that should *stay* narrow (labels) the explicit width, leave the column that should *fill* (fields) unconstrained, and pin the `NSGridView`'s own width to its container (`widthAnchor.constraint(equalTo: stack.widthAnchor)`) so the fill column has a definite total to fill. (3) A standalone `NSWindow` with `contentViewController` assigned (as opposed to embedded in the shared app-shell body) keeps re-deriving its own frame from its content's Auto Layout fitting size for as long as that content contains a **required equality** chain with no independent lower bound (cockpit-native-host-pages) - not just once at open. Confirmed live (`FM_DEBUG_HOSTEDITOR`-style probe: `setFrame`/`display:true` right after, then read `win.frame` again after a runloop tick): capping-and-centering the host editor's form column with a required `stack.widthAnchor == content.widthAnchor - 48` (paired with a `<=520` cap) made AppKit snap the *whole window* back to 568pt (the one width where that equality has zero slack) within one layout pass, even right after an explicit user resize to 1000pt - the window was effectively stuck. Swapping that one `==` for `>=`/`<=` inequalities (keep the `<=520` cap, position with `leadingAnchor >=`/`trailingAnchor <=`/`centerXAnchor ==` instead of a width tie) removed the trap entirely - verified holding any width the user drags to. This is *why* `HostEditorController`'s max-width-centered column uses inequalities, and it generalizes: any future standalone window with Auto-Layout-capped content needs the same inequality-not-equality shape, or it will silently refuse to stay resized. (4) An `NSScrollView`'s document view belongs width-pinned to `scroll.contentView.widthAnchor` (the clip view), never `scroll.widthAnchor` (the outer view) - caught in code review, not by inspection. With "Show scroll bars: Always" (System Settings, the default with a mouse attached), a non-overlay vertical scroller reserves a real ~15pt track that narrows the clip view without narrowing `scroll`'s own frame; pinning to `scroll.widthAnchor` lets the document view's trailing edge render underneath that track. `HostEditorController`, `FleetController`, and `ReviewController` (the last two fixed in cockpit-native-fixes5) all use `scroll.contentView.widthAnchor`; `SettingsController`'s otherwise-identical scroll setup still has the `scroll.widthAnchor` version and needs the same fix whenever it's next touched. (5) In a horizontal `NSStackView` row (icon + title/subtitle text + a trailing status pill and/or buttons), leaving every subview's compression resistance at its AppKit default (750, all equal) means a long title squeezes *every* subview roughly proportionally under narrow width, not just the title - and since the trailing pill/buttons are themselves small containers with edge-pinned labels, squeezing them below their fitting width can visually read as the badge "wrapping" even though nothing is a wrapping label (cockpit-native-settings-compact, `FleetController`'s PR/task rows). Fix: give the icon and every trailing control `.required` compression resistance (and hugging) so they never shrink, and give the title/subtitle text stack `.defaultLow` compression resistance so it's the one thing that truncates first. Generalizes to any row mixing fixed-size chrome with variable-length text. (6) `NSViewController.dismiss(_:)` is a documented no-op unless the view controller was presented via `presentAsSheet`/`presentAsModalWindow`/`presentAsPopover` (or has a `presentingViewController`) - for a plain top-level window whose `contentViewController` was just assigned directly (`HostEditorController`'s Save/Cancel/Delete, presented by `AppDelegate.presentHostEditor`), `dismiss(self)` silently does nothing. Fixed (cockpit-native-host-form-fixes) by closing the window directly (`view.window?.close()`) instead; verified live via `NSButton.performClick(nil)` on the located button plus a `win.isVisible` before/after check (no Accessibility permission needed - `performClick` runs the exact target/action path a real click does). Any future standalone-window view controller needs the same direct-close pattern, not `dismiss(self)`. (7) A second regular `NSWindow` opened while another app window is full screen docks into that same full-screen Space as a full-width tile by default (macOS's standard behavior for a second standard window) - this is what turned `HostEditorController`'s centered form back into a full-width layout, but only in full-screen mode. Fixed by setting `win.collectionBehavior = [.fullScreenAuxiliary, .moveToActiveSpace]` and `win.level = .floating` on the host editor's cached window in `presentHostEditor`, so it floats over the full-screen Space instead of tiling into it. Verified live via a temporary env-var-gated probe (`window.toggleFullScreen(nil)` then dump `win.frame` vs `screen.frame` - width stayed 640pt against a 1512pt screen, not stretched to match). Any future utility window opened over a possibly-full-screen main window needs the same `collectionBehavior`/`level` pair. (8) `NSVisualEffectView` with `.sidebar` material and `.behindWindow` blending mode - used for a real split-view sidebar, where it blends against the desktop through the window's own edge - renders an incorrect tint when used as a *full-size* destination or standalone window root, since `.behindWindow` blending composites against whatever is behind the *window* (desktop/other apps), not other content inside the same window (cockpit-native-theme-audit-review; same root cause independently hit and fixed for the Hosts sidebar in PR #18's Fix 6, then for the icon rail, `KeysSidebarController`, and `SnippetsController` in this task). Forcing the view's own `.appearance` does not fix it - only removing the vibrancy material does. Any full-size destination or standalone window's root should be a plain `NSView` with `wantsLayer = true` and a `HelmTheme`-derived `layer.backgroundColor`, exactly like `HostsSidebarController`/`FleetController`/`ConsoleController` already do; reserve real `NSVisualEffectView` sidebar material for an actual split-view pane with narrower, non-full-window geometry. (9) A plain `NSView()` used as an `NSScrollView`'s document view is **not flipped** by default, so y=0 is its *bottom*, not its top - while `FleetController`/`ReviewController` pin their `NSStackView` content to the document's *top* anchor (so the header is the visually topmost, highest-y arranged subview), a document shorter than the scroll's viewport (true before their background `gh`/Bitbucket fetch populates the section stacks) rests against the *bottom* of the clip view by default, leaving a blank gap the size of the shortfall sitting above the header - this is the "empty black area above the header for several seconds" bug (cockpit-native-loading-state). Confirmed live with a temporary geometry probe: with a plain `NSView`, the header sat at y=438 in a 668pt-tall viewport with 254pt of content (668-254=414, matching the gap exactly); swapping in `SettingsController`'s existing `FlippedView` (`override var isFlipped: Bool { true }`) pinned the header to y=24 and kept it there from frame 0 through 3s of simulated load. `SettingsController` already used this `FlippedView` + an explicit `scrollToTop()` in `viewWillAppear` (its own earlier Fix 4) - `FleetController`/`ReviewController` were simply built without carrying that pattern forward. Any new `NSScrollView`-backed destination needs the same `FlippedView` document view + `scrollToTop()` pair, or it will silently reproduce this bug the moment its content starts smaller than the viewport (e.g. during an async data load, or a genuinely short list). Follow-up (cockpit-native-fixes5): a captain report of the gap recurring specifically on the *first-ever* Overview visit after a cold launch could not be reproduced via extensive live instrumentation (geometry, `needsLayout`/`needsDisplay` flags, and appearance-transition timing all measured correct at every checked point, both on the first `isHidden`-toggle-triggered `viewWillAppear` and after the async `render()` grows the document height while already visible) - see that task's PR description for the actual probe transcripts. `viewWillAppear` and the end of `render()` in both controllers now force `view.layoutSubtreeIfNeeded()` immediately before `scrollToTop()` regardless, closing the two theoretical gaps the investigation could identify (a first-ever layout pass racing the automatic appearance notification; the scroll position never being re-pinned after the loading-skeleton's short content is replaced by full-height data) even though neither could be proven to be the captain's exact cause.
- **Contrast: `HelmTheme.mutedInk(_:)` is the one "muted/secondary text" token (fixes4 Fix 8).** Alpha-blending `chromeInkHex` at whatever opacity looks right in a dark palette (0.4-0.65 were previously scattered through `FleetController` and the now-removed placeholder stub) can silently drop well below WCAG AA (measured as low as 3.33:1) in the four *light* palettes, since the same alpha lands very differently against a light vs. dark backdrop. `mutedInk` is a measured-safe floor (0.7, worst case ~5.06:1 across all 8 themes' `backgroundHex`/`chromeBackgroundHex`) - route new muted text through it rather than picking an alpha ad hoc. Also: any view that uses `.secondaryLabelColor`/`.tertiaryLabelColor` or other system-semantic colors MUST force its root view's `.appearance` to `.darkAqua`/`.aqua` matching the active Helm theme inside its `ThemeManager.shared.observe` callback (see the `HelmTheme.swift` bullet below) - `FleetController` and `HostEditorController`'s standalone window both skipped this, so their secondary text resolved against the OS's actual light/dark setting instead of the in-app theme, going near-invisible whenever the two disagreed.
- **Toast.swift is the one save-confirmation pattern (fixes4 Fix 5).** `Toast.show(in:message:)` drops a small auto-dismissing pill at the top of any view. Host saves show it on `AppShellController.view` (not the host editor's own view, since that window closes immediately on Save); key saves show it directly on `KeysSidebarController.view` (that window stays open, sheet-presented editor closes on top of it).
- **`HelmTheme.swift` holds 12 palettes as concrete sRGB hex**: `helm-dark`/`helm-light`, hand-pinned originals, plus 10 more from cockpit-theme-overhaul (see the dedicated bullet below). **`ThemeManager.swift` is the single source of truth for which theme is active**, persisted by `HelmTheme.id` (migrates a pre-nav-redesign "fm.themeMode" dark/light pref once) - `ConsoleController` and every other theme-aware window/view (`HostsSidebarController`, `KeysSidebarController`, `SnippetsController`, `IconRailController`, `TopBarController`, `SettingsController`, `ReviewController`, `FleetController`, and every sheet editor) call `ThemeManager.shared.observe { ... }` rather than tracking their own copy - see the checklist at the top of `ThemeManager.swift` and `NSWindow.followHelmTheme()` (for a window's own title-bar chrome, which has no view to hook) for the full convention this task established after finding and fixing the same class of bug three separate times. `observe` returns a discardable `ThemeObservation` token (cockpit-native-host-pages) - every one of those call sites is an app-lifetime singleton and can ignore it, but `ConsoleController` keeps its token and passes it to `ThemeManager.shared.unobserve` inside `shutdown()`, since Fix 1 made it the first view controller in this app that can be deallocated mid-session (a deleted host's dedicated page) - skipping that would leak a dead closure into `ThemeManager.observers` on every delete. A window/view using `NSVisualEffectView` (or plain semantic system colors) needs its `appearance` forced to `.darkAqua`/`.aqua` in that observer for its colors to resolve against the Helm mode instead of macOS's own light/dark setting - `SettingsController` needed this added when it moved from its own floating window into the shared, always-themed body area (nav-redesign task). `ThemeMenu.swift` builds the shared DARK/LIGHT-grouped, swatch + checkmark `NSMenu` used by both the topbar's theme button and Settings' Appearance row.
- **`fm/cockpit-theme-overhaul` replaced the 6 formula-generated palettes (Midnight/Graphite/Nocturne/Paper/Frost/Linen) with 10 real, independently-sourced themes across 5 families, each contributing a light+dark pair: Solarized, Catppuccin, Gruvbox, Tokyo Night, Rosé Pine** (`solarized-dark`/`-light`, `catppuccin-mocha`/`-latte`, `gruvbox-dark`/`-light`, `tokyo-night-dark`/`-light`, `rose-pine-main`/`-dawn`). Every value is either lifted verbatim from that family's own canonical repo or a deliberate, documented deviation (inline code comments on each theme cover the specific hex and why) - see `data/cockpit-theme-research/report.md` for the full source URLs, per-family contrast tables, and the reasoning behind every deviation, rather than re-deriving it from the code comments alone. `HelmTheme.pairId` names each theme's light/dark counterpart within its own family; `ThemeManager.toggle()` (the quick dark/light flip, ⌘⌥T) looks that up and flips within the family instead of always landing on `helm-dark`/`helm-light` (those two still pair with each other, unchanged). The Settings Appearance grid (`SettingsController.rebuildAppearanceGrid`) wraps theme cards into fixed-width chunks (`Array.chunked(into:)`) rather than one ever-widening row per mode, so it doesn't need updating again the next time a theme family is added.
- **`AppSettings.swift` + `SettingsController.swift` (⌘, / App menu > Settings…, or the rail's gear icon) are the native Settings panel** - three cards, matching the web app's Connection/Appearance/Terminal visual weight (icon + title headers, generous spacing, no Sign-in card - native has no login): **Connection** (mirror-target field + a live `TmuxMirror.listSessions()` session-picker of selectable cards, plus the working-directory chooser); **Appearance** (the 8 `HelmTheme.allThemes` as a DARK/LIGHT-grouped wrapping grid of preview cards - swatch bars + name + checkmark - built by `SettingsController.rebuildAppearanceGrid`, the same source of truth `ThemeMenu` uses for the topbar picker); **Terminal** (font-size presets 12/13/14/16 via `ConsoleController.stepFontSize`, plus three `NSSwitch` toggles: "Reconnect automatically" - `AppSettings.autoReconnect`, read by `ConsoleController.processTerminated` to actually re-run a dropped tab's launch after a short delay instead of only showing the ⌘R hint; "Bell & notifications" - `AppSettings.notifyOnNeedsDecision`, driving `FleetNotifier`'s background poll of `FleetDataSource.parseTasks()` to post a real macOS notification the first time a task newly needs the captain; and session-logging-by-default, moved here from the old General section). `FM_SHELL_CWD`/`FM_MIRROR_TARGET`/`FM_LOG_SESSIONS_DEFAULT` env vars still win when set, `AppSettings` (backed by `UserDefaults`) is the fallback.
- **`SudoTouchIDData.swift` + Settings' fourth card, "Security" (cockpit-settings-sudo-touchid)** added a live "Touch ID for sudo" row. Its status reads `/etc/pam.d/sudo` and `/etc/pam.d/sudo_local` directly (Automic Vault's `av harden sudo` hardener has no `--json` listing entry the way the gh/aws hardeners do, since it's a root-only, file-content-driven check) - a direct Swift port of `automic-vault/src/isotopes/hardeners/sudo.rs`'s `pam_tid_enabled`/`line_enables_pam_tid`. A real caveat worth knowing: the appended `auth sufficient pam_tid.so` line only takes effect if `/etc/pam.d/sudo` already `include`s `sudo_local` - not true on every Mac - so the row has a distinct "Unavailable" state for that case rather than assuming it's always configured. The Enable button runs `sudo av harden sudo` (not the plain unprivileged form, which only previews and prints "next: sudo av harden sudo") through the same one-shot Console command-tab mechanism (`ConsoleController.openCommandTab` via `AppShellController.runInConsole`, wired from `settings.onRunCommand`/`onRunCommandTracked` exactly like `bootstrap.onRunCommand`) since a real `sudo` password prompt needs a real terminal. The row itself is rebuilt (not just re-themed) on every status change, mirroring Bootstrap's `ghAuthRow` - since `SettingsController`'s other cards are built once and never torn down, this is the first dynamic row in that file, and its own `rebuildSecuritySection` prunes `hoverRows` of the just-removed row's now-orphaned `HoverHighlightView` entry before appending the new one, so that shared re-theming list doesn't grow unbounded across repeated status checks. **`fm/cockpit-settings-sudo-nixdarwin` added a `.notEnabledNixDarwin` status** for nix-darwin-managed Macs: `/etc/pam.d/sudo_local` there is a symlink chain into the immutable Nix store (confirmed live: `/etc/pam.d/sudo_local` -> `/etc/static/pam.d/sudo_local` -> `/nix/store/.../etc-sudo_local`), and `av harden sudo` refuses to write through a symlink (a correct guard against symlink attacks on root-owned PAM files), failing with a bare ELOOP instead - even bypassing that would be pointless, since nix-darwin regenerates the file from the flake on every rebuild anyway. `SudoTouchIDSource.isManagedByNixDarwin()` detects this via `(path as NSString).resolvingSymlinksInPath` (the full-chain resolve, not just the first `readlink` hop - same idiom `DotfilesData.swift` already uses for its own symlink checks) landing under `/nix/store/`. The row's fix on that class of machine is guidance-only text pointing at nix-darwin's own `security.pam.services.sudo_local.touchIdAuth = true;` option, mirroring the gh row's `isotopeMissingPlainGhInstalled` guidance-only case - the app never edits the captain's dotfiles itself. The status *check* (reading file content for `pam_tid.so`) is unaffected by any of this, since reading through a symlink is transparently fine; only the write path `av harden sudo` uses is what breaks.
- **Find (Fix 4, cockpit-native-ui-fixes2).** The topbar Search pill / its ⌘K calls `AppShellController.activateConsoleFind()`, which brings Console forward and calls `ConsoleController.showFind()` - the exact same in-terminal find the console toolbar's own magnifying-glass icon triggers (SwiftTerm's native find bar on the active tab). There is one search behavior in the app, not two; it has nothing to do with Hosts.
- **Verifying native UI bugs without a real screenshot.** This machine has a real console session, but the agent's shell process (`claude` under WezTerm) has neither Screen Recording nor Accessibility permission granted, and there is no way to grant either non-interactively (`screencapture`/`osascript "System Events"` both fail, TCC.db is SIP-protected even to root, no passwordless sudo). The effective substitute that actually caught real bugs (fixes4): add temporary, env-var-gated debug code straight into the controller under test - print live `NSView.frame` geometry after a real layout pass, or a `Thread.callStackSymbols` dump inside a suspect delegate callback - `swift build && FM_DEBUG_X=1 .build/debug/FirstmateCockpit`, read the printed numbers/stack, then revert the instrumentation before committing. This is real evidence from AppKit's actual layout/event engine, not a screenshot, but it is not guesswork either - say so explicitly in any PR that relies on it instead of claiming a visual check that didn't happen.
- **The app bundle lives at `dist/Manjesh Grand Line.app` (gitignored, renamed from `Firstmate.app` in the cockpit-rebrand-grand-line task).** Built by `native/build_native_app.sh` (plain `swift build -c release` + manual bundle assembly, no py2app, bundle ID `com.firstmate.cockpit.native` - deliberately unchanged by the rename, see below). There used to be a second, older `dist/Firstmate.app` produced by a since-removed Python/WKWebView app via the root `build_app.sh` - that codebase and script are gone, so this is the only app and the only build path now. **Codesigning (cockpit-stable-signing):** the script signs the assembled `.app` with a local self-signed identity named exactly "Firstmate Cockpit Local Dev" (`codesign --force --sign "Firstmate Cockpit Local Dev" --identifier "$BUNDLE_ID"`) when that identity exists in the login keychain (`security find-identity -v -p codesigning`), and otherwise warns and continues unsigned rather than failing - this keeps the app's code identity, and therefore Keychain ACL trust for saved SSH keys (see the "SSH keychain" bullet above), stable across rebuilds instead of getting a new ad-hoc identity every time. See `native/README.md`'s "Local signing setup" section for the one-time per-machine commands to (re)create this cert. **The bundle ID (`com.firstmate.cockpit.native`), the signing identity name (`Firstmate Cockpit Local Dev`), and the Swift module/target name (`FirstmateCockpit`) are all deliberately unchanged by the app's display-name rebrand to "Manjesh Grand Line"** - only user-visible text (window titles, tooltips, button labels, the bundle's `CFBundleName`/`CFBundleDisplayName`) changed. Changing the bundle ID or signing identity would invalidate the captain's already-saved Keychain-backed SSH keys.
- **"Firstmate Latest Updates" (`UpdatesController.swift` + `UpdatesData.swift`, cockpit-native-updates-page, restyled in cockpit-updates-redesign).** A `.updates` rail destination (the captain's own hand-drawn artwork as of cockpit-native-updates-polish - see the `CaptainIcon.swift` bullet below; pinned directly above Settings the same way Settings is pinned above the avatar - see `IconRailController.loadView`'s bottom-anchored chain) listing every tool in the captain's ecosystem (npm-global `*-axi` packages, Homebrew `gh`/`tmux`/the `claude-code` cask, herdr, no-mistakes, and firstmate's own upstream sync), each checked automatically on page load and updatable with one explicit click - Settings-styled cards/rows, per-row spinner + expandable command-output log, a `Toast` + `UNUserNotificationCenter` notification on success. cockpit-updates-redesign (captain-supplied HTML mockup, translated to native AppKit + Helm colors rather than embedded) added: a 4-tile summary strip above the tool list (Tools Installed / Up to Date / Updates Available / Last Checked, the last a self-ticking relative-time label via a `Timer` started in `viewWillAppear`/stopped in `viewWillDisappear`, reusing the same rounded-card chrome as `card(icon:title:content:)`/`SettingsController.card` via the shared `cardBackgrounds` list rather than inventing a second style); a live `NSSearchField` above the cards that hides non-matching rows by name substring and collapses a whole category card once none of its rows match (`UpdatesController.CategorySection`/`applyFilter` - same `NSSearchFieldDelegate.controlTextDidChange` pattern as `HostsSidebarController`'s host filter); a per-category-tinted rounded icon tile behind each row's glyph (`tintHex(for:)`, drawn from the active theme's own ANSI slots/accent, never a literal hex); and each row's status pill restyled into a clearer badge (fuller pill padding, Title Case text). The page-level "Check all" action **reverses** cockpit-native-updates-polish's earlier bare-icon-button decision: the captain's mockup showed it as the page's clear primary action, so it is now a prominent, labeled, accent-filled pill ("Refresh", built as a plain `NSView` + `NSClickGestureRecognizer` - the same clickable-styled-card pattern as `SettingsController.themeCard`/`sessionCard` - rather than fighting `NSButton`'s tight default padding on a borderless button) using `theme.selectionTextHex` for its icon/label color, since that token is already contrast-verified against an opaque `accentHex` fill (SwiftTerm's own selected-text-on-accent pairing). It still swaps for the same determinate `NSProgressIndicator` + "Checking… (N/M)" label while checks run, then the same `Toast` on completion - only the idle-state chrome changed, not the behavior. Two non-obvious things any future change to this page should know: (1) `UNUserNotificationCenter.current()` throws an uncaught `NSException` ("bundleProxyForCurrentProcess is nil") when the running process has no real `Info.plist`/bundle identifier - true for the README's own documented `swift run`/bare `.build/debug/FirstmateCockpit` dev workflow - so `UpdatesController.notify` guards on `Bundle.main.bundleIdentifier != nil` first; the packaged app (a real bundle) is unaffected either way. (2) `no-mistakes --version`'s own output ("no-mistakes version v1.37.0 …") contains a decoy "v" inside the word "version" before the real "v1.37.0" token - parse whitespace-separated tokens shaped `v<digit>...`, not the first bare "v" in the string (caught live: the naive version crashed every check silently). no-mistakes has no `--check` flag; its outdated-version banner only prints on stderr from a real subcommand's `--help` (itself a safe no-op), which is what's parsed instead. herdr similarly has no "check without updating" flag - latest-available is read from Homebrew (herdr happens to be homebrew-core tapped here) while the actual Update action still runs herdr's own `herdr update`, never `brew upgrade herdr`. Firstmate's own row shells out to `bin/fm-sync-upstream.sh` (a firstmate-repo file, out of scope for this cockpit repo to commit - see the PR that introduced this page for its exact content) and gracefully reports `.notInstalled` if that script isn't present yet, rather than failing.
- **`CaptainIcon.swift` (cockpit-native-updates-polish) embeds a raster rail icon as a base64 PNG literal, not an `Assets.xcassets` catalog.** This project builds with `swift build` only (Swift Package Manager, CLT, no Xcode project) - `xcrun --find actool` (the tool that compiles asset catalogs) is absent from a CLT-only toolchain, confirmed on this machine, so `.xcassets` is not viable here. An SPM `resources:` bundle was also considered and rejected: `MetalTerminalRenderer.swift`'s own `candidateBundles()` comment documents that this *executable* target's generated `Bundle.module` accessor `fatalError`s (aborts the process, not throws) if it can't resolve the resource-bundle path, and this app's manual bundle assembly (`build_native_app.sh`, no py2app/Xcode step) isn't guaranteed to lay resources out where that accessor looks. A base64-embedded PNG has no bundle path to resolve at all - works identically under `swift run` and any assembled `.app`. Any future non-SF-Symbol rail/UI icon should follow the same pattern: extract/process the asset with `native/scripts/extract-captain-icon.py` as a worked example (luminance/local-contrast thresholding via Pillow), verify the output's alpha channel programmatically (nonzero-but-not-fully-opaque pixel count) before embedding, then wrap it in an `enum` exposing a `static let templateImage: NSImage?` with `isTemplate = true` so it tints via `contentTintColor` exactly like an SF Symbol.
- **The `.updates` rail destination's user-visible title is "Updates"** (cockpit-bootstrap-scaffold renamed it from "Firstmate Latest Updates" - the only `RailDestination` title that repeated the app's own name and ran three words). The bullet above still refers to it by its old task-history name; the page and rail icon itself say "Updates".
- **`.bootstrap` (cockpit-bootstrap-scaffold, phase 1 of a larger, captain-reviewed plan) is a rail destination pinned between Updates and Settings**, following the identical "pinned above Settings" convention `.updates` already established (`IconRailController.loadView`'s bottom-anchored chain; see the `RailDestination` enum's doc comment). `BootstrapController.swift` is scaffold-only as of this phase - a single "Firstmate home" card that lets the captain override where `FirstmateHome.resolve()` looks (`AppSettings.fmHome`, checked after `FM_HOME`/`FIRSTMATE_HOME` and before the hardcoded fallback candidates) via an editable path field, a `NSOpenPanel` "Browse…", and a "Save & Verify" that validates the candidate with `FirstmateHome.homeOk(at:)` before persisting - `FirstmateHome.root` is a `static let` resolved once at launch, so a save only takes effect after the explicit "Restart Manjesh Grand Line" action it offers, never live. Later phases (not yet built) add a software-install checklist and an Updates-page behavior split - see the task that introduced this scaffold for the full plan.
- **Phase 2 (cockpit-bootstrap-dotfiles) added the dotfiles/machine-config card and the global-agent-instructions verification section to `.bootstrap`.** The captain's real machine config is a Nix flake at `~/manjesh/dotfiles` (nix-darwin + home-manager + nix-homebrew), symlinked to the well-known `~/.dotfiles` by that repo's own `bootstrap.sh`/`rebuild.sh`. `DotfilesData.swift` is the model side - live `git`/file checks only, no hardcoded status - mirroring `UpdatesData.swift`'s `run`/`resolveExecutable` `Process` plumbing for git remote/branch/`status --short`, and parsing/rewriting the live `user = "..."` line out of `flake.nix` rather than a literal name in source. Every symlink check (managed items + the three `AGENTS.md`/`CLAUDE.md` targets) fully resolves the chain with `(path as NSString).resolvingSymlinksInPath` (a property, not a method - `readlink -f` equivalent), not just the first hop: confirmed live that `home.nix`'s `mkOutOfStoreSymlink` targets go through an intermediate `/nix/store/.../home-manager-files/...` hop before landing on the real repo file. `~/.zshrc` is deliberately expected to show "not linked" even when everything is fine - its `home.nix` entry is `programs.zsh` (home-manager *generates* the file), not a `home.file` source symlink, so it resolves straight into the Nix store with no repo path in the chain at all. Any action that can invoke `darwin-rebuild switch` (clone+`bootstrap.sh`, `rebuild.sh`, and Part B's "Create link", which just re-runs `rebuild.sh` since home-manager owns creating those links on every switch) needs `sudo`'s interactive TTY prompt, so all of them run through `ConsoleController.openCommandTab(label:command:cwd:)` (a `.shell` launch running `$SHELL -lc "<command>"`, opened in the shared Firstmate console via `AppShellController.runInConsole` - `BootstrapController.onRunCommand`, wired in `AppShellController.loadView`, mirrors the existing `onPresentHostEditor` closure-injection pattern) rather than a silent background `Process`. **`TabModel.isOneShotCommand` (fm/cockpit-bootstrap-reconnect-fix)** marks a tab created by `openCommandTab` so `ConsoleController.processTerminated` never schedules the `AppSettings.shared.autoReconnect` reconnect for it - a plain `.shell` tab and a one-shot provisioning command share the same `TabLaunch.shell` launch kind, so without this flag a successful `rebuild.sh` exit looked identical to a dropped interactive shell and triggered an infinite re-run (captain-reproduced live: endless `darwin-rebuild switch` + repeated sudo prompts). Any future one-shot command tab needs the same flag set at its `addTab` call site.
- **Phase 4 (cockpit-bootstrap-full-setup) added a "Run full setup" card at the top of `.bootstrap`** - a single button that sequences the three cards below it for a blank-machine setup, showing a shared ordered progress list (pending/running/done/skipped/failed per step: Firstmate home, dotfiles & machine config, agent instructions, software). It is a pure sequencer, not a reimplementation: every step calls the same method its card's own button already calls. Two small additions made that possible without duplicating logic: (1) `TabModel.onOneShotCompletion` + a `completion` param on `ConsoleController.openCommandTab`/`AppShellController.runInConsole` (paired with `BootstrapController.onRunCommandTracked`, wired alongside the existing `onRunCommand`) let the sequencer wait for a Console command's *real* exit (`processTerminated`'s one-shot branch) before starting the next step, rather than a fixed timer - needed for the dotfiles step, since `rebuild.sh`/`bootstrap.sh` can run for a while and starting software installs concurrently would race two provisioning steps. (2) The software checklist card gained a card-level "Install everything missing" button (`BootstrapController.installAllMissing`, running one row at a time - never concurrently, to avoid racing two `brew`/`npm` invocations against the same lock file) sitting alongside the existing per-row "Install" buttons; both now funnel through one `performInstall(_:completion:)`. The Firstmate-home step is a pure status check (`FirstmateHome.homeOk`) with no auto-clone - a failure there stops the whole sequence and points at the home card's own Save & Verify flow, per the task brief's explicit requirement not to guess a path. The dotfiles step is never skipped (idempotent scripts), unlike the other three which skip once already satisfied.
- **Phase 3 (cockpit-bootstrap-software) added a "Software checklist" card to `.bootstrap` and split the Updates page's `.notInstalled` row behavior.** The checklist card is a thin view over the existing `DependencyCatalog`/`UpdatesSource` in `UpdatesData.swift` - no separate catalog or check/update logic - grouped by `DependencyCatalog.categoryOrder`, each row calling `UpdatesSource.check`/`.update` on a background queue and re-rendering the whole card on completion (`BootstrapController.rebuildSoftwareSection`), the same brute-force full-rebuild pattern the file's other two dynamic sections already use rather than per-row incremental updates. `UpdatesController`'s `.notInstalled` rows no longer show "Update" (which used to silently mean install-in-place); they show a distinct link-styled "Install in Bootstrap →" (`UpdateRow.installInBootstrapButton`) that calls `onNavigateToBootstrap`, wired by `AppShellController` to `show(.bootstrap)` - every other status's Update button is byte-for-byte unchanged. **A proposed new catalog entry (a third-party Homebrew cask, "Automic Vault") was deliberately NOT added**: the brief asked for it to be tapped and installed as part of implementing this task, but the tap/cask (`automic-vault/isotopes/automic-vault`) was unfamiliar and unverifiable - a real cask definition, but downloading a DMG from a GitHub org with no way to check what it contains. Treated as an unverifiable supply-chain risk and declined; flagged as `needs-decision` in the task's status file rather than silently included or silently dropped. Any future request to add an unfamiliar third-party install source to this catalog deserves the same scrutiny before adding it, regardless of how the request is framed.
- **cockpit-bootstrap-automic-vault added that deliberately-omitted "Automic Vault" catalog entry** (category "Security") once the captain confirmed it's their own tool, not an unknown third party - corroborated independently by `projects.md`'s fleet-registered `automic-vault` project entry and a separate week-earlier `playbook-automic-vault` task, not just the follow-up task's own say-so. This surfaced a real Homebrew quirk worth knowing for any future fully-qualified `owner/tap/cask` catalog entry: `brew info`/`brew upgrade` both accept the fully-qualified token fine, but `brew list --versions --cask <fully-qualified token>` fails (exit 1) even once installed - Homebrew tracks an installed cask by its short token only. Confirmed live (installed automic-vault for real, then diffed `brew list --versions --cask automic-vault/isotopes/automic-vault` (fails) against `brew list --versions --cask automic-vault` (succeeds)). Fixed once, generically, in `UpdatesData.swift`'s shared `installedBrewVersion` - the cask branch now looks up by the token after the last `/` - rather than as a one-off for this entry, so it's already correct for the next fully-qualified cask added here. `ensureTapped` (same file) similarly runs `brew tap <owner/tap>` unconditionally before every cask check/update, a no-op once already tapped, since a fully-qualified cask's tap has to exist before *any* brew subcommand resolves it.
- **cockpit-bootstrap-not-synced added the final planned `.bootstrap` card, "Not synced here, by design"** (`NotSyncedData.swift` + `BootstrapController.rebuildNotSyncedSection`), at the bottom of the page, below the software checklist and never wired into the "Run full setup" sequencer - exactly as the original plan specified. Three rows: SSH private keys and `.env`/secrets/tokens are permanently static explanatory text with no button (by design - the cockpit's own Keychain-backed key store is `ThisDeviceOnly`, `.env` is gitignored like firstmate's own); GitHub/`gh` CLI auth is the one row with a real action, since Automic Vault's `av harden gh` (automic-vault repo's `src/isotopes/hardeners/gh_cli.rs`/`.md`) is a real credential-migration command tied to secret-gate id `"gh"`. The row's live status comes from `av hardeners --json`'s per-tool `hardened` boolean (confirmed live - exits 0 either way, unlike `av doctor gh`, which exits 1 whenever its issues array is non-empty and is otherwise equivalent); the button runs `av harden gh` through the same one-shot Console command-tab mechanism (`onRunCommand`/`onRunCommandTracked`, already generic - no `AppShellController` changes needed) the other Bootstrap actions use, since it can prompt interactively. If `av` itself isn't on `PATH`, the row says so and points at the Software checklist card instead of showing the button. Confirmed live on this machine: `av` is installed but the patched `gh-cli` isotope (`brew install automic-vault/isotopes/gh-cli`, required by the hardener before it will touch anything) is not, so `hardeners --json` reports `gh` as not-hardened and clicking the button surfaces `av`'s own "gh-cli is not installed" message in the Console tab - real CLI output, not a fabricated status.
- **`fm/cockpit-bootstrap-vault-hardeners` added AWS, Codex, and Homebrew rows to the same card and fixed the gh row's real prerequisite gap.** `NotSyncedSource.checkHardened(name:)` (`NotSyncedData.swift`) is now the one place that calls and parses `av hardeners --json`; `checkGhHardening()`/`checkAwsHardening()` layer one extra real state on top of it (`GhHardenState`/`AwsHardenState`), `checkCodexHardening()`/`checkHomebrewHardening()` return the shared `HardenerStatus` directly. **gh fix:** `automic-vault/isotopes/gh-cli` conflicts with the plain `gh` Homebrew formula ("Conflicts with: gh" per `brew info automic-vault/isotopes/gh-cli`), so a captain whose dotfiles still declare plain `gh` in `homebrew.brews` got a bare "gh-cli is not installed" failure with no path forward; `ghIsotopeMissingWithPlainGhInstalled()` detects that specific case (isotope path missing, plain `gh` resolvable) and the row shows guidance pointing at `configuration.nix`'s `homebrew.brews` + `rebuild.sh` instead of a doomed retry button. **AWS row:** `av harden aws` migrates the existing `default` profile out of `~/.aws/credentials`, so `hasLocalAwsDefaultCredentials()` distinguishes "nothing to harden yet" (no `default` section, confirmed live on this machine) from a real not-hardened state - showing an action button only when there's something to act on. **Codex row:** `av harden codex` refuses to run while ChatGPT.app is open (shared config between Codex CLI and the desktop app); that's surfaced as body text, not just left as a failure after the fact. **Homebrew row:** the most disruptive of the four per `homebrew.md` (completions gone, most casks incompatible, `PATH`/shell-startup change required) - the row shows that disruption as body text plus a bold warning line and requires an explicit checkbox (`homebrewDisruptionAcknowledged`) before its Harden button enables, unlike the other three rows' single-click action. **Live version-skew finding, not a bug in this code:** this machine's installed `av` (2.10.0) has no `codex` entry in `hardeners --json` at all, even though the reviewed source checkout (`automic-vault` repo, `Cargo.toml` version 3.0.0) has a real `codex.rs` hardener - `checkHardened(name: "codex")` correctly falls through to its existing `.checkFailed(...)` branch rather than crashing, surfacing the version gap honestly. Upgrading `av` on a given machine should resolve it without any cockpit-side change.
- **`fm/cockpit-bootstrap-dotfiles-pull` made the dotfiles card actually fetch from GitHub before applying, and fixed its clone URL after the captain's real dotfiles repo moved from `manjesh-raj/dotfiles` to `manjesh-raj/manjesh-config`.** `DotfilesSource.cloneURL` (`DotfilesData.swift`) is the one hardcoded pointer to the captain's real dotfiles repo - update it there if the repo ever moves again, nowhere else. `DotfilesRepoState.commitsBehindOrigin` (`nil` when unknown - no network, no remote, never coerced to `0`) is computed by a real `git fetch origin <branch>` followed by `git rev-list --count HEAD..origin/<branch>` - deliberately not trusting whatever stale remote-tracking ref already happened to be on disk. `BootstrapController`'s three rebuild-triggering actions (`runRebuildClicked`, `createLinkClicked`, and `runSetupStepDotfiles`'s already-exists branch) all now go through one shared `Self.rebuildCommand(repoPath:)` that runs `git pull --ff-only` before `./rebuild.sh`, inside the same one-shot Console command tab as before (`isOneShotCommand` still applies - see the reconnect-loop fix bullet above). `--ff-only` is the load-bearing choice: it fast-forwards silently when the local checkout is a clean ancestor of `origin`, but aborts with a real, visible git error (never silently forced or silently skipped) the moment the local checkout has diverged - verified live in a scratch repo pair (clean fast-forward pulled a remote-only commit; a deliberately diverged pair aborted with exit 128 and left local commits/files untouched). The "behind origin" state is surfaced as its own banner (`buildBehindOriginBanner`, `ansiHex[3]`/warn-colored) separate from the existing uncommitted-changes banner (`ansiHex[1]`/error-colored) - the two conditions are independent and can both be true at once.
- **`HelmUIComponents.swift` (cockpit-modern-ui-settings, phase 1 of a captain-reviewed HTML/CSS mockup UI modernization) holds the two shared visual helpers every restyled page reuses: `HelmTint` + `IconTileView`, and `HoverHighlightView`.** `HelmTint` generalizes per-category tinting (`.accent`/`.info`/`.good`/`.warn`/`.critical`/`.violet`/`.neutral`, each resolving to one of a `HelmTheme`'s own `accentHex`/`ansiHex` slots - never a literal hex) into one place; `IconTileView` is the ~34x34pt, ~9pt-corner-radius "icon in a softly tinted square" view built on top of it, with an `applyTheme(_:)` re-tint hook for `ThemeManager.shared.observe` callbacks. `HoverHighlightView` is an `NSTrackingArea`-backed `NSView` that animates `layer.backgroundColor` between caller-supplied `normalColor`/`hoverColor` on mouse enter/exit via `NSAnimationContext` (0.12s), swapping to an instant set when `NSWorkspace.shared.accessibilityDisplayShouldReduceMotion` is on; callers pick both colors from the active theme, e.g. via the paired `NSColor.hoverShifted(by:forMode:)` (blends toward white on dark themes, black on light ones - the same "blend toward whichever endpoint has headroom" idea `Dimming.swift` uses for terminal contrast, applied here to derive a hover shade from a real theme color). `SettingsController` was the first consumer (phase 1: its three card headers, the Appearance grid's theme-preview cards - a real 3-color bg/surface/accent swatch matching the mockup's `.theme-swatch` structure - and the Connection tab's session-picker cards and toggle/button rows). **Phase 2 (cockpit-modern-ui-updates) applied the same two helpers to `UpdatesController`**: each tool row's icon square is now an `IconTileView` (tinted via a `categoryTint(for:) -> HelmTint` mapping - npm packages `.info`, Homebrew `.warn`, Other tools `.neutral`, Security `.violet`, Firstmate `.accent` - replacing the page's own now-removed `tintHex(for:) -> String`), and each row's container is a `HoverHighlightView` (`normalColor = .clear`, `hoverColor` a `chromeLineHex`-alpha shade, set in `applyThemeToRow`). The page also gained a real "All / Needs attention" segmented filter (two plain clickable `NSView`s, matching this codebase's established custom-pill-control pattern rather than `NSSegmentedControl`) that combines with the existing live search field in `applyFilter()` - "Needs attention" reuses `DependencyStatus.showsUpdateButton` (the same predicate the stats strip's "Updates Available" tile already counts) rather than a second definition, and `applyFilter()` is re-run from `render(_:)` on every status change so a Check/Update that flips a row's status keeps the active filter's visible set correct without polling. Bootstrap was intentionally left untouched by this phase. Both phases verified across all 8 `HelmTheme.allThemes` (including light/dark) via a temporary `FM_DEBUG_SETTINGS_SWEEP`/`FM_DEBUG_UPDATES_SWEEP`-style launch hook cycling `ThemeManager.shared.setTheme` on the target page and checking for AutoLayout warnings/crashes - not a screenshot (this environment has no Screen Recording/Accessibility permission, see the "Verifying native UI bugs" bullet above), reverted before commit each time. **Phase 3 (cockpit-modern-ui-bootstrap) reframed Bootstrap's four sequenced cards (Firstmate home, dotfiles, agent instructions, software checklist) as one connected vertical stepper**, `StepDotView` (a plain layer-backed circle - number or checkmark, deliberately not built on `IconTileView`, since a step dot is never an arbitrary SF Symbol) plus a `StepRowViews` struct per step holding the dot/line/title/chip/detail views. "Not synced here, by design" stayed a separate card below the stepper, unchanged, exactly as speced. The one load-bearing decision here diverges from every earlier dynamic section in this file: **the stepper's row scaffolding is built once in `loadView` and mutated in place (`refreshStepperVisuals`), never torn down and rebuilt** like `dotfilesStack`/`agentStack`/`softwareStack`'s own `clearStack`-then-repopulate convention - because a step's `stepContentBox` wraps one of those *already-persistent* stack views, and recreating the wrapper on every refresh would silently reparent that stack view into a brand-new box each time, leaving the old box's now-dangling leading/trailing/top/bottom constraints attached to a view that moved to a different superview (never crashes, just piles up dead constraints indefinitely - caught by reasoning about the reparenting, not by hitting the bug live). Each step's done/current/pending state is derived live every time (`stepIsDone`, reading `FirstmateHome.homeOk`/`dotfilesRepoPath`/`agentItems`/`softwareRows` - the same fields every other Bootstrap section already reads), never hardcoded; `refreshStepperVisuals` is called from `rebuildSoftwareSection` (via `defer`, covering every code path that can flip a step's doneness: initial check, install, and the shared `rebuildDynamicSections` sweep that also fires on theme change). The "Run full setup" bar was restyled to the mockup's icon-tile + title/subtitle + progress-track look (subtitle and fill width both derived from `setupSteps`, e.g. "Step 2 of 4 — {title}" while running) with its actual sequencing logic (skip-if-satisfied, stop-on-failure, real Console-tab waiting) byte-for-byte unchanged; the detailed per-step pass/fail list (`setupStack`) stayed put underneath the bar for failure-reason detail the compact bar can't show. Verified via the same temporary `FM_DEBUG_BOOTSTRAP_SWEEP`-style launch hook pattern as phases 1-2, this time delayed 5s before sweeping themes so the real background dotfiles/software checks complete first and exercise the new code path with live data, not just its initial loading state.
- **`ToolRowLayout` (`HelmUIComponents.swift`, cockpit-bootstrap-software-row-parity) factors the "tool checklist row" chrome - icon tile, name/detail text stack, a caller-populated trailing-controls stack, a disclosure chevron, and an expandable command-output log - out of `UpdatesController`'s per-tool rows into one place shared with `BootstrapController`'s software checklist rows.** `ToolRowLayout.Views` holds the concrete view instances (an `IconTileView`, labels, pill, `NSStackView`/`NSButton`/log field/container, and a `HoverHighlightView` row container); `.build(...)` assembles them and `.applyTheme(...)`/`.pill(...)`/`.setLogExpanded(...)` re-theme/update them, all idempotent (checking `superview` before re-adding a subview) so both callers' very different row lifecycles work against the same functions - `UpdatesController` builds each row's views once and mutates them in place, while `BootstrapController.rebuildSoftwareSection()` tears down and rebuilds fresh view instances on every check/install/theme change (its long-standing `clearStack`-then-repopulate convention), which is why `BootstrapController`'s per-item `SoftwareRowState` now also persists `log`/`isLogExpanded` on the model instead of the view - the view itself doesn't survive a rebuild, so the expand state has to. `DependencyCatalog.tint(for:)` (`UpdatesData.swift`) is the one category->`HelmTint` mapping both pages read, replacing `UpdatesController`'s former private copy. Bootstrap's software-row Install button changed from being *added only* when `.notInstalled` (so an installed row showed no button at all) to *always present*, with only its title/enabled state driven by `installButtonTitle`/`installButtonEnabled`. Verified live via a temporary env-gated launch-hook probe (walking the Bootstrap view hierarchy for buttons matching a `DependencyItem.id`, across all 8 `HelmTheme.allThemes`) that every row's button stays in the same stack position and never disappears; every catalog item was already installed on the dev machine at verification time, so the probe exercised the "installed -> disabled" branch live but not a real "not installed -> Install enabled" transition - reverted before commit, per the "Verifying native UI bugs" convention above. **`fm/cockpit-bootstrap-row-width-parity` extended `ToolRowLayout.build` with a `showDetails: Bool = true` parameter** so a caller with nothing to expand (no `CheckOutcome.log`) can get the icon-tile/name/detail/pill row without the disclosure chevron or log panel - `detailsTarget`/`detailsAction` became optional (`nil` when `showDetails` is false). Bootstrap's "Managed items" (`managedItemRow`), "Global agent instructions" (`agentInstructionRow`), and "Run full setup" step summary (`setupStepRow`) all moved onto this shared component (`showDetails: false`) instead of each hand-rolling a `[label, pill]`/`[label, pill, button]` `NSStackView` - the same fix also resolved a visible "card too narrow" bug: those hand-rolled rows used a horizontal `NSStackView` at the AppKit default `.gravityAreas` distribution, which doesn't fill available width (see gotcha (10) above) the same way `UpdatesController`'s per-row chevron did before `cockpit-bootstrap-software-row-parity` fixed it there. `agentInstructionRow`'s "Create link" button also picked up the same always-present/disabled-when-already-linked treatment `cockpit-bootstrap-software-row-parity` gave the software checklist's Install button, instead of being added to the row conditionally.

- **`.docs` (cockpit-docs-viewer) is a rail destination pinned directly above `.updates`** showing the captain's DevOps Playbook (`https://manjesh-raj.github.io/devops-playbook/`, source repo `manjesh-raj/devops-playbook`) in-app. The site is a real interactive app (canvas diagrams, tab switchers, local-storage reading progress in its `script.js`) that needs a real `WKWebView` - but the captain does not want a live network dependency for it, so `DocsController.swift` loads a bundled, synced-on-demand local copy via a `file://` URL (`WKWebView.loadFileURL`) rather than the live site, and its `WKNavigationDelegate` cancels any navigation that leaves the local synced folder, reopening it in the system browser via `NSWorkspace.shared.open` instead - this panel can never become a general browser. An empty state (no local copy yet) offers the same sync action described below. `DocsData.swift` owns where the copy lives (`DocsStore.folderURL`, defaulting to `~/Library/Application Support/FirstmateCockpit/docs/`, override `FM_DOCS_DIR` - same convention as `HostStore`/`SSHKeyStore`, deliberately outside the `.app` bundle for the same `Bundle.module` reason as `CaptainIcon.swift`) and the sync contract (`DocsSyncSource.check()`/`.update()`, wired into `DependencyCatalog` as a new "Documentation" category/"DevOps Playbook" row on the Updates page, following the exact same `CheckOutcome`/`UpdateOutcome` contract every other row uses): **check** compares the local `.synced-commit` sidecar file against the live repo's current `main` commit SHA (GitHub REST, `GET /repos/.../commits/main` - a public repo, so no `gh`/token is required, though see the auth bullet below for why one is used opportunistically); **update** re-downloads the repo root's current file listing (GitHub Contents API, so new/removed pages are picked up automatically) into a fresh temp directory and swaps it into place atomically via `FileManager.replaceItemAt` before recording the new SHA, so a `WKWebView` reload racing an in-progress sync never sees a half-written folder. `DocsSyncCenter` (`DocsData.swift`) is a tiny observer list (mirrors `ThemeManager.observe`'s shape) that `DocsController` uses to reload its `WKWebView` the moment a sync completes, whether triggered from the Updates page or the Docs page's own empty-state button.
- **`DocsSyncSource`'s GitHub requests opportunistically authenticate via `gh auth token` (`fm/cockpit-docs-auth-github-api`).** Captain-reproduced live: an unauthenticated request to `api.github.com` shares GitHub's 60/hour-per-IP quota with every other unauthenticated caller on the machine (`curl`/`gh`/etc.), so unrelated traffic earlier in the day exhausted it and the Documentation row failed with a real 403 and `x-ratelimit-remaining: 0`, even though the repo itself is genuinely public and needs no credentials to read. `DocsData.swift`'s `get(_:)`/`downloadBytes(from:)` now call a small `ghAuthToken()` helper (same `Process`-based shell-out convention as `UpdatesData.swift`'s `run()`, not a new networking abstraction) before every request and add `Authorization: Bearer <token>` when it returns one; `ghAuthToken()` returns `nil` - never throws - if `gh` isn't installed or isn't authenticated, in which case the request goes out exactly as before. Fetched fresh per request rather than cached, and nothing is persisted: no new Keychain entry, app setting, or credential storage was added for this.
- **"SRE Lead" (`fm/cockpit-sre-lead`) is a toolbar toggle on a dedicated host page's toolbar (`ConsoleController.swift`, gated on `!isFirstmateConsole` since the shared Firstmate console has no single host cluster to investigate) - not a rail destination.** Clicking the `SRELeadStatusPill` slides open a right-hand pane (`ConsoleController`'s `sreLeadPane`/`sreLeadPaneWidthConstraint`, animated via `NSAnimationContext` + `layoutSubtreeIfNeeded`) that talks to a locally-run Claude Code session investigating the connected host's Kubernetes cluster. **`fm/cockpit-sre-lead-shared-terminal` replaced the tool's whole execution model for kubectl commands - it no longer opens a second SSH connection at all.** Read-only enforcement still lives in exactly one place, `native/Scripts/sre_kubectl_mcp.py`: a dependency-free, stdlib-only MCP stdio JSON-RPC server exposing one tool, `kubectl_readonly`, whose subcommand is checked against a hardcoded `{get, describe, logs, top, events}` set and whose every argument is checked against a conservative safe-character allowlist, before the command ever reaches anywhere it could run - `--allowedTools` also restricts the agent to that one tool plus `Task`/`TodoWrite`, so it has no path to a raw Bash/Read/Write tool either. **Where a command now actually runs: the captain's own already-authenticated interactive tab for that host** - not a second connection, a local kubeconfig, or any credential this app holds. `SRELeadBridge.swift` is a small file-based request/response protocol between this Python script and the Swift app (different processes, no shared memory): the script writes `request-<id>.json` (the validated, `shlex.quote`'d kubectl command) into a per-session `bridge/` directory (`SRELeadSession.bridgeDir`), `SRELeadBridge`'s main-thread poll timer notices it, injects the command into the host page's one `primarySSHTab` (set once, by `ConsoleController.connectSSHIfNeeded`, and never guessed at again even if that tab is later duplicated - see the "Dedicated host pages" bullet for why there's normally only one anyway) wrapped in two fresh random markers (`echo <start>; <command>; echo <end>`, via the same `TerminalView.send(txt:)` `Snippet`'s "Run" action already uses), polls that tab's own `Terminal.getBufferAsData()` for the end marker, extracts everything between the two markers as the real output (searching only buffer content appended after injection, so stale scrollback or an unrelated earlier marker can never be mistaken for this call's own output - see `SRELeadBridge.checkInFlight`), and writes `response-<id>.json` back; the script polls for that file and returns it. **Two concurrency guards, both real, both tested** (`SRELeadBridgeSelfTest.swift`): a request is refused outright (not queued) if another is already in flight, or if the tab shows recent captain activity (`CockpitTerminalView.lastUserActivity`, tracked via a local `NSEvent` monitor scoped to this view as first responder, since `TerminalView.keyDown(with:)` is `public`, not `open`, and can't be overridden from this module) either just before injection or at any point before the end marker appears - the latter discards the (possibly-corrupted) output rather than ever returning it as trustworthy. **Verified live in this environment** (no real EKS bastion available here): `SRELeadBridgeSelfTest.run()` (`FM_RUN_SRE_LEAD_BRIDGE_TESTS=1 .build/debug/FirstmateCockpit`) exercises the whole Swift-side polling/extraction/busy-detection surface against a `FakeBridgeTerminal` stand-in; `native/Scripts/test_sre_kubectl_mcp.py` covers the Python-side request/response/timeout logic and re-verifies validation is untouched, including a raw JSON-RPC `tools/call` reconfirmation that `subcommand=delete` is rejected before any request file is written. **Not verified here** (no real cluster/bastion in this sandbox): the actual end-to-end path against a real host - a live interactive tab logged all the way through a real password-gated bastion, SRE Lead calling kubectl_readonly, and real output coming back - which remains the captain's own manual acceptance bar, same as every prior attempt.
- **`fm/cockpit-sre-lead-ux-fixes` fixed three UX problems from the captain's first live end-to-end test of SRE Lead (the bridge mechanism itself worked - real kubectl output flowed through and a real analysis came back).** (1) **Scrollback loss on open/close**: `loadView`'s constraints used to pin `content.trailingAnchor` to `sreLeadPane.leadingAnchor`, so animating the pane's width also resized `content` - and any frame change on a SwiftTerm view triggers `resize(cols:rows:)`, which reflows the buffer at the new column count and can truncate scrollback. `content` is now pinned to the root's full width unconditionally; `sreLeadPane` overlays its right edge (it's added after `content`, so it already rendered on top) instead of pushing it, so toggling the pane never resizes any terminal. Confirmed live with a temporary `FM_DEBUG_SRE_LEAD_SCROLLBACK`-style probe (per the "Verifying native UI bugs" convention below): filled a real shell tab with 300 lines, snapshotted the buffer, opened then closed the pane, snapshotted again - byte-for-byte identical, reverted before commit. (2) **Persona narrated the investigation instead of leading with the finding**: `SRELead.persona` (`SRELead.swift`) now explicitly instructs findings-first, terse-by-default replies with investigation detail (what was checked, what was ruled out, tool-limitation hedging) available only on request - not a vague "be concise" tweak. Real reply quality against this new prompt still needs the captain's own next live test to confirm; the diagnosis of the anti-pattern came from the real transcript of the first live test. (3) **The pane showed a raw `claude` TUI, not a native UI**: the pane used to run an interactive `claude` session in a detached tmux server, mirrored into the pane exactly like the Firstmate Mirror tab (`TmuxMirror.setUp(target: session.tmuxSessionName)`) - showing the captain someone else's CLI chrome (permission-mode banner, box-drawing borders) instead of anything native to this app. That whole tmux/mirror path is gone: `SRELead.setUp` only prepares the MCP config + a scratch/working directory now (no tmux session, no wrapper script - `Process`'s `arguments` array reaches `claude` directly, sidestepping the wrapper-script-for-shell-escaping trick the old tmux path needed for the multi-line persona). `SRELeadRunner.swift` runs one non-interactive `claude -p ... --output-format json` process per question/follow-up and threads conversation context across turns via `--resume <session_id>` (confirmed live, both via `claude --help` and a real two-call local test, that `-r`/`--resume` works with `-p`/`--print`) - `--output-format json` rather than `stream-json`, deliberately: the pane only ever needs the assistant's one final reply per turn, not partial/incremental text or intermediate tool-call events, so the simpler single-object parse was chosen over a streaming line-event parser that would have been harder to validate without a real cluster. `SRELeadChatView.swift` renders the conversation as a native message feed (`NSScrollView` + `NSStackView` of plain text blocks, styled via `HelmTheme`/`FlippedView`) with a single-line input field at the bottom - the same "type your first question" entry point the old tmux pane had, now as a real input field instead of clicking into a raw TUI. `SRELeadBridge.swift`/`sre_kubectl_mcp.py` and the read-only guarantee above are unaffected by any of this - only how the pane renders and how `claude` is invoked changed, not how kubectl commands reach the shared terminal.
- **tmux env propagation gotcha (`fm/cockpit-sre-lead-env-fix`): a tmux server only captures its global environment once, at the moment it first starts, from whichever client spawned it.** Every `new-session -d` against that same server afterward - exactly what `SRELead.setUp` calls on each SRE Lead spawn - inherits that originally-captured environment, NOT the `env:` dict passed to that later `TmuxMirror.run` call, unless the session overrides it itself. So `childEnvironmentDict()`'s PATH fix-up (adding `/opt/homebrew/bin` etc.) only actually took effect the first time a session was created against a fresh server; every later SRE Lead spawn against an already-running server (e.g. a leftover from earlier testing) silently got stuck with whatever PATH that server happened to capture at its own startup, causing the spawned `claude` session's SessionStart hooks to fail with `command not found` for Homebrew-installed tools (`gh-axi`, `lavish-axi`, `chrome-devtools-axi`) even though those tools were genuinely on disk. Reproduced live: started a tmux server with a deliberately stripped PATH, then `new-session -d`'d a second session against it - `which gh-axi` failed (exit 1) using only the `env:` dict, and succeeded once the wrapper script exported PATH itself. Fixed by making `SRELead.setUp`'s generated wrapper script self-sufficient - it now has its own `export PATH=...` line (built from `childEnvironmentDict()`'s resolved value) before `exec`ing `claude`, so it's correct regardless of what the server captured; `TmuxMirror.run(..., env:)` is kept as-is since it's still correct for a fresh server and harmless otherwise. Any future code that spawns a `new-session` against a tmux server that might already be running (not just this file's `TmuxMirror`/`SRELead`) needs the same self-sufficiency, since passing an `env:` dict to `new-session`/`run` alone is not reliable.
- **`fm/cockpit-sre-lead-reply-formatting` fixed the captain's next complaint: replies were findings-first (per the ux-fixes bullet above) but still rendered as one flat, unstyled paragraph, with no explicit conclusion/recommendation structure - "no highlights, no bold, no blocks, no conclusion, no recommendation for the next action."** Two connected pieces, one fixed marker convention shared between them. **Persona (`SRELead.persona`, `SRELead.swift`):** a substantive investigation reply must now be two labeled paragraphs, in this exact literal wording, with no blank line between a label and the rest of its sentence - `**Finding:** <conclusion + minimal evidence>` first, then an optional `**Recommended next action:** <concrete next step>` (omitted only when there genuinely isn't one, e.g. a pure lookup). A short acknowledgement/clarifying-question reply doesn't need this structure. **Rendering (`SRELeadMarkdown.swift` + `SRELeadChatView.swift`):** an assistant message's text is parsed by `SRELeadMarkdown.parse` into blocks (paragraph/bulletList/codeBlock/callout) and each gets a real AppKit view instead of one plain label - bold renders semibold, `` `code` `` spans get a monospace font + tinted background, `- ` lists render as an actual indented bullet list, and a `**Finding:**`/`**Recommended next action:**` paragraph renders as a distinct rounded callout (a `ToolRowLayout.pill`-style label over the body, on a panel tinted with `HelmTint.accent`/`.good` respectively) rather than plain bold text. Block/list/code-fence structure and inline bold/code-span detection both come from Foundation's own `AttributedString(markdown:, options: .full)` parser (confirmed live it already tags runs with the block structure and inline emphasis/code needed here) - not a hand-rolled markdown parser; only the block-grouping-by-`presentationIntent`-identity and the Finding/Recommendation label detection are hand-rolled, since those are app-specific and no generic parser could know about them. **The exact label text is a fixed contract between the persona prompt and `SRELeadMarkdown`'s `findingLabel`/`recommendationLabel` constants** - changing one without the other doesn't break anything visibly (the text still renders, just as a plain paragraph instead of a callout), so keep them in sync deliberately. Parsing is covered by a permanent self-test (`SRELeadMarkdownSelfTest.swift`, run via `FM_RUN_SRE_LEAD_MARKDOWN_TESTS=1 .build/debug/FirstmateCockpit`, same convention as `SRELeadBridgeSelfTest.swift`); the block-to-NSView rendering (geometry, non-zero widths/heights across a light and a dark `HelmTheme`) was verified with a temporary `FM_DEBUG_SRE_LEAD_MARKDOWN_RENDER`-style probe per the "Verifying native UI bugs" convention below, reverted before commit.
- **Five attempts (PRs #70-73, plus an abandoned PTY investigation) tried to make SRE Lead's kubectl tool work over a *second*, independent SSH connection to the same bastion, before the captain confirmed why that entire direction was a dead end.** In order: `fm/cockpit-sre-lead-become-user` (PR #70) added a per-host `sudo su - <user> -c '<kubectl ...>'` escalation for bastions where the login user can't run kubectl directly; `fm/cockpit-sre-lead-su-syntax-fix` (PR #71) reordered it to `sudo su -c '<kubectl ...>' - <user>` per util-linux's own `su` synopsis, and the captain's live test showed `-c` was silently ignored entirely on the real bastion regardless of order; `fm/cockpit-sre-lead-su-stdin-pipe` (PR #72) switched to piping the command into the escalated shell's stdin instead of a `-c` argument, which worked for the escalation itself; `fm/cockpit-sre-lead-startup-snippet` (PR #73) then found that every one of those escalations had been running on the *wrong machine* - the "EKS Preprod Bastion" host's `ssh` argv only reaches a jump/gateway box, with the real target (and `kubectl`) another hop away via the interactive tab's own startup snippet - and added sequential stdin writes over a persistent shell to reach it. **All of this became moot once the captain confirmed a hard constraint that no amount of argument-shape or extra-hop fixing could ever have worked around: the real bastion's EKS Bastion hop is username/password-gated by policy, with no SSH key auth possible there - so a second, independent, fully-automated connection can never complete that login chain, because nothing can supply a password that isn't stored anywhere, by design.** `fm/cockpit-sre-lead-shared-terminal` replaced the whole model with a shared-terminal bridge instead (see the "SRE Lead" bullet above and `SRELeadBridge.swift`) - `Host.becomeUser` and the ssh-argv/host-config-JSON plumbing this history describes are gone from the codebase entirely, not just unused. **Do not resurrect a second-connection approach for this or any other password-gated hop** - the lesson isn't "fix the escalation argument shape," it's "a second automated connection cannot finish a login that requires a password no one gave it."

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise.
